package network.thezone.yace.core.board;

import network.thezone.yace.core.Piece;
import network.thezone.yace.core.Side;

/*
 * This class should encapsulates the interface towards the board
 * representation, so it should hide the details of the implementation whether
 * it is piece-centric or square-centric or both...
 */

public interface Board {

    //return? getPosition(Piece piece, Side side);
}
